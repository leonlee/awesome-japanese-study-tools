# 贡献指南

本项目使用 [VitePress](https://vitepress.dev) 发布，如果你有兴趣实时预览或者参与维护工作，请按照下面的方法构建开发环境：

```bash
# 安装项目依赖
npm install

# 启动服务
npm run docs:de

# 预构建
npm run docs:build

# 本地预览
npm run docs:preview
```
